import styled from 'styled-components'

export const Main = styled.main`
	margin: 0 auto;
	max-width: 680px;
	padding: 2rem;
`

export const Below = styled.div`
	color: var(--dark);
	margin: 0 auto;
	max-width: 680px;
	padding: 2rem;
`
